# Firebase Configuration (from serviceAccountKey.json)
# You'll need to get these values from Firebase Console > Project Settings > General > Your apps
# For Web App, add a web app if you haven't already and copy the config

# The project_id can be extracted from serviceAccountKey.json
# The rest need to come from the Firebase Console web app config
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=teachersync-bc2e8
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=teachersync-bc2e8.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id_here

# Note: You need to create a web app in Firebase Console and get the actual values
# Or the messaging system will work in a "fallback" mode showing config errors
